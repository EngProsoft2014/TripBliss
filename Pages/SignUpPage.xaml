<?xml version="1.0" encoding="utf-8" ?>
<Controls:CustomControl xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TripBliss.ViewModels"
             xmlns:Controls="clr-namespace:TripBliss.Controls"
             xmlns:ex="clr-namespace:TripBliss.Extensions"
             x:Class="TripBliss.Pages.SignUpPage"
             Title="SignUpPage"
             IsBusy="{Binding IsBusy}"
             NavigationPage.HasNavigationBar="False" 
             BackgroundColor="White">
    <!--<Controls:CustomControl.BindingContext>
        <vm:SignUpViewModel></vm:SignUpViewModel>
    </Controls:CustomControl.BindingContext>-->
    <ScrollView VerticalOptions="FillAndExpand">
        <VerticalStackLayout Margin="30,10,30,30">
            <Image Source="https://iconape.com/wp-content/png_logo_vector/bntu.png" WidthRequest="140" ></Image>
            <Border StrokeShape="RoundRectangle 20" StrokeThickness="1" Stroke="Gray" VerticalOptions="Center" Margin="0,10,0,0">
                <VerticalStackLayout Margin="20" Spacing="20">
                    <!--<Image>
                        <Image.Source>
                            <FontImageSource Glyph="" FontFamily="FontIconSolid" Color="Gray"></FontImageSource>
                        </Image.Source>
                    </Image>-->
                    <Border StrokeShape="RoundRectangle 30" StrokeThickness="1" Stroke="Gray">
                        <Picker ItemsSource="{Binding LstCompanyType}" ItemDisplayBinding="{Binding Name}" SelectedItem="{Binding OneCompanyType,Mode=TwoWay}"
                            Title="{ex:Translate Name=pickerTitleCompanyType-SignUpPage}" Margin="10,2" FontFamily="Almarai-Regular"
                            FontSize="14" TextColor="Black" TitleColor="#adadad" HorizontalOptions="CenterAndExpand">
                        </Picker>
                    </Border>
                    <Border StrokeShape="RoundRectangle 30" StrokeThickness="1" Stroke="Gray">
                        <Entry Keyboard="Text" TextColor="Black" FontSize="14" FontAttributes="Bold" Placeholder="{ex:Translate Name=plcCompanyName-SignUp}" HorizontalTextAlignment="Center"
                           Margin="10,2" Text="{Binding CompanyName}"></Entry>
                    </Border>
                    <Border StrokeShape="RoundRectangle 30" StrokeThickness="1" Stroke="Gray">
                        <Entry Keyboard="Email" TextColor="Black" FontSize="14" FontAttributes="Bold" Placeholder="{ex:Translate Name=plcCompanyEmail-SignUpPage}" HorizontalTextAlignment="Center"
                           Margin="10,2" Text="{Binding CompanyEmail}"></Entry>
                    </Border>
                    <Border StrokeShape="RoundRectangle 30" StrokeThickness="1" Stroke="Gray">
                        <Entry Keyboard="Numeric" TextColor="Black" FontSize="14" FontAttributes="Bold" Placeholder="{ex:Translate Name=plcCompanyPhone-SignUp}"
                           HorizontalTextAlignment="Center" Margin="10,2" Text="{Binding CompanyPhone}"></Entry>
                    </Border>
                    <Border StrokeShape="RoundRectangle 30" StrokeThickness="1" Stroke="Gray">
                        <Entry Keyboard="Email" TextColor="Black" FontSize="14" FontAttributes="Bold" Placeholder="{ex:Translate Name=plcEmail-SignUpPage}" HorizontalTextAlignment="Center"
                           Margin="10,2" Text="{Binding LoginRequest.Email}"></Entry>
                    </Border>
                    <Border StrokeShape="RoundRectangle 30" StrokeThickness="1" Stroke="Gray">
                        <Entry Keyboard="Text" TextColor="Black" FontSize="14" FontAttributes="Bold" Placeholder="{ex:Translate Name=plcUserName-Login}" HorizontalTextAlignment="Center"
                           Margin="10,2" Text="{Binding LoginRequest.UserName}"></Entry>
                    </Border>
                    <Border StrokeShape="RoundRectangle 30" StrokeThickness="1" Stroke="Gray">
                        <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" Margin="10,2">
                            <Entry x:Name="entryPassword" Keyboard="Text" IsPassword="True" TextColor="Black" FontSize="14" FontAttributes="Bold" Placeholder="{ex:Translate Name=plcPassword-Login}"
                           HorizontalTextAlignment="Center" Text="{Binding LoginRequest.Password}" HorizontalOptions="CenterAndExpand"></Entry>

                            <Image x:Name="imgEyePass" Margin="0,0,10,0" HorizontalOptions="End">
                                <Image.Source>
                                    <FontImageSource FontFamily="FontIconSolid" Glyph="" Color="Gray" Size="15"></FontImageSource>
                                </Image.Source>
                                <Image.Triggers>
                                    <DataTrigger TargetType="Image" Binding="{Binding Source={x:Reference entryPassword},Path=IsPassword}" Value="False">
                                        <Setter Property="Source">
                                            <Setter.Value>
                                                <FontImageSource FontFamily="FontIconSolid" Glyph="" Color="Gray" Size="15"></FontImageSource>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Image.Triggers>
                                <Image.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"></TapGestureRecognizer>
                                </Image.GestureRecognizers>
                            </Image>
                        </StackLayout>
                    </Border>
                    <Button Text="{ex:Translate Name=lblSignUp-Login}" BackgroundColor="#2FA562" TextColor="White" WidthRequest="140" CornerRadius="20" Margin="0,15,0,0"
                         Command="{Binding ClickRegisterCommand}" CommandParameter="{Binding LoginRequest}"></Button>
                </VerticalStackLayout>
            </Border>
            <Label Margin="0,15,0,0">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="{ex:Translate Name=lblHaveAccount-SignUp}" TextColor="Gray" FontSize="14" FontFamily="Almarai-Regular"></Span>
                        <Span Text=" " TextColor="Gray" FontSize="14" FontFamily="Almarai-Regular"></Span>
                        <Span Text="{ex:Translate Name=lblSignIn-SignUp}" TextColor="Orange" FontSize="14" FontFamily="Almarai-Regular"
                              FontAttributes="Bold">
                            <Span.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnSignIn"></TapGestureRecognizer>
                            </Span.GestureRecognizers>
                        </Span>
                    </FormattedString>
                </Label.FormattedText>
            </Label>
        </VerticalStackLayout>
    </ScrollView>
</Controls:CustomControl>